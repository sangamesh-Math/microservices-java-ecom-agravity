spring:
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:dev-redis.internal.vpc}
      port: ${SPRING_DATA_REDIS_PORT:6379}
      timeout: 2000ms

cart:
  ttl-hours: 72

logging:
  level:
    com.ecommerce: DEBUG
